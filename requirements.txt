# --- 코어 (형상·회로·분배) ---
pydantic>=2.5
numpy>=1.24
scipy>=1.11

# --- 로컬 서버 ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- STEP 생성 (OCC 커널, 설치 용량 큼) ---
cadquery>=2.4

# --- 냉매 물성 (없으면 rho, mu 를 직접 지정) ---
CoolProp>=6.6

# --- 개발/검증 ---
pytest>=8.0
